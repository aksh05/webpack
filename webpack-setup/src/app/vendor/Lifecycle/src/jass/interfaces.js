// import Perf from 'react-addons-perf'; // ES6
// import * as Redux from "redux";
// import 'setimmediate';
// import * as reduxPersist from 'redux-persist';
// import reduxPersistConstants from 'redux-persist/constants'
// import reduxThunk from "redux-thunk";
// import logger from "./redux/middleware/logger.js";
// //Batching action creator and associated higher order reducer for redux that enables batching subscriber notifications for an array of actions.
// import { batchActions, enableBatching } from 'redux-batched-actions';
// //React bindings for Redux.
// import * as reactRedux from "react-redux";

// //routing library
// import page from "page";

// //for data storage
// import localforage from "localforage";

import Virtual from "./react/Virtual.js";
// import VirtualDom from "./react/VirtualDom.js";
// import VirtualCSSTransitionGroup from "./react/VirtualCSSTransitionGroup.js";
// import PropTypes from 'prop-types';

export default {
    // Redux,
    // reduxPersist,
    // batchActions,
    // enableBatching,
    // actionTypes: {
    //     reduxPersist: reduxPersistConstants
    // },
    // reduxThunk,
    // logger,
    // page,
    // localforage,
    // Provider: reactRedux.Provider,
    // connect: reactRedux.connect,
    Virtual,
    // VirtualDom,
    // VirtualCSSTransitionGroup,
    // Perf: process.env.NODE_ENV == "dev" ? Perf : null,
    // PropTypes
}
